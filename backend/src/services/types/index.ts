export type {Feedback, Role, MessageContent } from "./message.type";
export type {ConversationDto } from "./conversation.types";

export type ServiceError = {
    message: string;
    code: number;
};

